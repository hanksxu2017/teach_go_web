<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.jeeweb.modules.sys.mapper.CourseMapper" >

    <update id="blankTeacher">
        delete tg_course set teacher_id = null where course_id = #{courseId}
    </update>
    <update id="relatedTeacher">
        delete tg_course set teacher_id = #{teacherId}, update_date = now() where course_id = #{courseId}
    </update>

</mapper>